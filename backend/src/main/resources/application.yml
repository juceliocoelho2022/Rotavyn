spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/rotavyn}
    username: ${DB_USER:rotavyn}
    password: ${DB_PASSWORD}
management:
  endpoints:
    web:
      exposure:
        include: health
