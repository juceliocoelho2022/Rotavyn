services:
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: rotavyn
      POSTGRES_USER: rotavyn
      POSTGRES_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD in .env}
    ports:
      - "5432:5432"
    volumes:
      - rotavyn_pg:/var/lib/postgresql/data
volumes:
  rotavyn_pg:
